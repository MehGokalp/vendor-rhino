language: go

jobs:
  fast_finish: true
  include:
    - name: "1.3 Stable version test"
      go: "1.13"
  allow_failures:
    - name: "Latest version test"
      go: master

go_import_path: github.com/MehGokalp/vendor-rhino

services:
  - mysql

script:
#  - go test -v ./...
  - bash ./scripts/gofmt.sh
  - bash ./scripts/coverage.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
